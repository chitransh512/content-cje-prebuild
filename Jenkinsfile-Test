pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
          script {  when {
		        expression {
		        return env.BRANCH_NAME == "origin/master"
                }   
            }
	    }
                sh """
                    ls -la
                    echo $env.BRANCH_NAME
                """
            }
        }
    }
}
