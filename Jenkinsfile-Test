pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
            when{
            env.BRANCH_NAME == "origin/master" || env.BRANCH_NAME == "origin/development"
            }
                sh """
                    ls -la
                    echo $env.BRANCH_NAME
                """
            }
        }
    }
}
