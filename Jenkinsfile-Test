pipeline {
    agent any
    
    stages {
        stage('For mastere') {
            steps {
            when(env.BRANCH_NAME == origin/master || env.BRANCH_NAME == origin/development)
                sh """
                    ls -la
                """
            }
        }
    }
}
